{
  "name": "thunderx",
  "version": "1.0.0",
  "description": "ThunderX — Neumorphic (Soft UI) web component library built with Lit",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:lib": "tsc && vite build --config vite.lib.config.ts",
    "preview": "vite preview"
  },
  "dependencies": {
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0"
  }
}
